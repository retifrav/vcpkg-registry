To use Dawn in your project:

    find_package(Dawn CONFIG REQUIRED)
    target_link_libraries(main
        PRIVATE
            dawn::dawncpp_headers
            dawn::dawn_native
            dawn::dawn_proc
            dawn::tint_api
            # there are many more targets available there,
            # you'll need find out whichever of those you need in your project
    )
