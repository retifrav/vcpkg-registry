@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(@WEBSOCKETPP_USING_ASIO_STANDALONE@)
    find_dependency(asio CONFIG REQUIRED)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake")

check_required_components(@PROJECT_NAME@)
