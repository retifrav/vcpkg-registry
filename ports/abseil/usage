To use Abseil in your project:

    find_package(absl CONFIG REQUIRED)
    target_link_libraries(main
        PRIVATE
            absl::any
            absl::log
            absl::base
            absl::bits
            # ...
    )
