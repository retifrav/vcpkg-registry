To use Abseil in your project:

    find_package(absl CONFIG REQUIRED)
    target_link_libraries(${PROJECT_NAME}
        PRIVATE
            absl::any
            absl::log
            absl::base
            absl::bits
            # ...
    )
